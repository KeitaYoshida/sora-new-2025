[build]
  command = "pnpm install --ignore-scripts=false && pnpm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20.4.0"
  NPM_FLAGS = "--version"
  PNPM_FLAGS = "--no-frozen-lockfile" 